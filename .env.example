# HubSpot API — same token as Unified Dashboard (get from Railway env vars)
HUBSPOT_ACCESS_TOKEN=

# Server
PORT=3000
NODE_ENV=development

# Database (optional — only needed if using PostgreSQL cache)
DATABASE_URL=
